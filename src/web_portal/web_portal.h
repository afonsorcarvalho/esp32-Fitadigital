/**
 * @file web_portal.h
 * @brief Servidor HTTP (AsyncWebServer) — configurações, console de logs, explorador SD.
 *
 * Sem autenticação (rede local). Porta 80.
 */
#pragma once

void web_portal_init(void);
